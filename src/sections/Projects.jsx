import SectionHeader from '../components/SectionHeader';import ProjectVisual from '../components/ProjectVisual';import {portfolio} from '../data/portfolio';
export default function Projects(){return <section id="work" className="section projects"><SectionHeader number="02" eyebrow="Trabalhos selecionados" title={<>PROJETOS<br/><i>SELECIONADOS</i></>}/><div>{portfolio.projects.map((p,i)=><article className={'project '+(i%2?'reverse':'')} key={p.slug} data-reveal><ProjectVisual project={p}/><div className="project-info"><div className="project-count">{p.number} / 03 <span>{p.category}</span></div><h3>{p.name}</h3><p className="project-summary">{p.summary}</p><p className="project-detail">{p.detail}</p><div className="project-meta"><div><small>ATUAÇÃO</small><b>FULL STACK</b></div><div><small>ANO</small><b>{p.year}</b></div></div><div className="stack">{p.stack.join(' · ')}</div><a className="text-link" href={p.github} target="_blank" rel="noreferrer">CÓDIGO-FONTE ↗</a></div></article>)}</div></section>}
